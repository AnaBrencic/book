const dbConfig = {
    dialect: "mysql",
    host: "localhost",
    username: "root",
    password: "98983632",
    database: "book-nodejs",
    define: {
        timestamps: true,
        underscored: true
    }
}

export default dbConfig;